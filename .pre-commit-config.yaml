repos:
  - repo: local
    hooks:
      - id: secret-leak-detector
        name: Secret Leak Detector
        entry: python scanner/cli.py scan --staged
        language: system
        pass_filenames: false
